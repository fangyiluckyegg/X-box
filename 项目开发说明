我有一个综合型的项目（项目根目录为D:\crh123dexiaohao\X-box\），基本上是基于容器开发，当前根据dev类开发环境文件是可以跑通的，希望能够参考dev类开发环境文件，实现prod类生产环境文件的完全对齐，请帮助进行系统性的bUG排查和代码整合精简优化。具体开发与目标生产环境如下
一、开发电脑配置与环境清单
CPU：i7-8700K
内存：16GB DDR4 3000MHz
显卡：RTX2070S（AI 推理加速）
存储：1TB NVMe 系统盘、3T+1T 机械盘
系统：Windows 11 专业版
环境：开发工具：VS Code（Docker/YAML/Vue/Java 插件）、Ollama+bge（AI基础，宿主机部署）
二、生产服务器配置与环境清单
1.硬件：mac mini m2 16+1T
2.系统：macOS Sonoma 14.8.7
3.环境：OrbStack2.1.3（docker容器编排）、Ollama+bge（AI基础，宿主机部署）

二、项目当前情况（要求开发与生产环境完全对齐）
根目录：D:\crh123dexiaohao\X-box\
X-box
├── gateway/nginx            # Nginx网关配置、路由、SSL证书
│   ├── conf.d               
│   │   └── prj.conf         # 业务HTTPS路由配置（移到此处，ssl目录只做挂载）【可上传GIT】
│   └── ssl                  # ssl挂载目录，仓库无证书文件
│       ├── *.key            # 【不能上传GIT】
│       ├── *.crt            # 【不能上传GIT】
│       └── *.pem            # 【不能上传GIT】
├── db
│   ├── mysql_data           # MySQL持久化数据【不能上传GIT】
│   ├── redis_data           # Redis持久化RDB文件【不能上传GIT】
│   ├── mysql_init           # prj数据库初始化SQL（建库、账号、权限）
│   │   ├── init.sql         # 有真实密码【不能上传GIT】
│   │   ├── init.template.sql # 无真实密码（把硬编码密码替换为占位符，仅保留建库、授权逻辑，团队统一共用）【上传GIT】
│   │   ├── migrate_role.sql # 无真实密码（把硬编码密码替换为占位符，仅保留建库、授权逻辑，团队统一共用）【上传GIT】
│   │   ├── msg.sql          # 无真实密码（把硬编码密码替换为占位符，仅保留建库、授权逻辑，团队统一共用）【上传GIT】
│   │   └── work.sql         # 无真实密码（把硬编码密码替换为占位符，仅保留建库、授权逻辑，团队统一共用）【上传GIT】
│   └── mysql_scripts        # 待核验
│       └── docker-entrypoint-wrapper.sh  # 待核验
├── web/prj-frontend       
│   ├── node_modules         # 是否可删除？已经换地方了。项目依赖包，体积巨大，通过package-lock.json统一安装【不能上传GIT】
│   ├── src                  # Vue前端源码【上传GIT】
│   ├── public               # Vue前端静态资源（如index.html-你的应用的首页和图片等）【上传GIT】
│   ├── .env.development     # 前端开发环境变量模板（仅接口域名、通用配置，无密钥则可提交）【上传GIT】
│   ├── .editorconfig        # 代码格式化、校验规则，团队统一规范【上传GIT】
│   ├── .eslintignore        # 代码格式化、校验规则，团队统一规范【上传GIT】
│   ├── .eslintrc.js         # 代码格式化、校验规则，团队统一规范【上传GIT】
│   ├── babel.config.js      # 代码格式化、校验规则，团队统一规范（可选）Babel的配置文件，用于编译JavaScript。【上传GIT】
│   ├── vitest.config.js     # 待核验
│   ├── vue.config.js        # 代码格式化、校验规则，团队统一规范（可选）Vue CLI项目的配置文件【上传GIT】
│   ├── package-lock.json    # 锁定依赖版本，必须提交【上传GIT】
│   ├── package.json         # 项目配置，记录项目元数据、依赖、脚本等【上传GIT】
│   ├── nginx.conf           # 前端生产Nginx托管配置【上传GIT】
│   ├── Dockerfile.prod      # 服务器生产环境镜像【可上传GIT】
│   ├── Dockerfile.dev       # 本地开发环境镜像【可上传GIT】
├── backend/prj_backend-c
│   ├── src                  # Spring Cloud Alibaba后端源码【上传GIT】
│   ├── target               # 【不传GIT】   
│   ├── pom.xml              # Maven主依赖配置，锁定所有Java三方包版本，强制提交，保证开发/生产依赖完全一致【上传GIT】
│   ├── Dockerfile.prod      # 生产环境-线上CI流水线专用多阶段构建镜像，仅保留JRE，无编译工具，用于打包纯净生产镜像【可上传GIT】
│   └── Dockerfile.dev       # 开发环境-单阶段JDK镜像，预装Maven/Python，供 VSCode Dev Containers 挂载源码调试）【待修改】
├── Niu_Txl
│   ├── 607                  # 待核验
│   └── 902                  # 待核验
│   ├── .dockerignore        # 保留：Docker build context 专属，不能并入根
│   ├── .gitignore           # 保留：子目录 Git 忽略，可并入根但保留更清晰
│   ├── Dockerfile.classphp  # PHP 8.2（注释已更正）
│   ├── index.php            # 重定向至 /902/message/index.php
├── logs                     # 全容器统一日志挂载目录【不传GIT】
│   ├── nginx                # 已有，正常
│   ├── mysql                # 【暂缺】
│   ├── redis                # 【暂缺】
│   ├── prj-php              # 已有，正常
│   ├── prj-backend-c
│   └── prj-frontend
├── scripts                  # 运维启停、备份、清理脚本
│   ├── prj_restart.bat      # Windows批处理启停脚本【可上传GIT】
│   ├── prj_stop.bat         # Windows批处理启停脚本【可上传GIT】
│   ├── backup_dev.bat       # 定时备份脚本【可上传GIT】
│   ├── setup-host-ollama.ps1  # Windows设备部署脚本
│   └── setup-host-ollama.sh # mac设备备部署脚本
├── backup                   # 数据库、配置文件备份存档【暂缺】
├── .devcontainer
│   ├── devcontainer-lock.json  # VS远程容器开发配置，绑定prj项目【上传GIT】
│   └── devcontainer.json    # VS远程容器配置，绑定prj项目【上传GIT】
├── docs
│   └── archive              # 待核验
├── .env.dev                 # 本地开发环境变量【不传GIT】
├── .env.backend             # 本地开发环境变量（针对后端容器）【不传GIT】
├── .env.dev.example         # 保留：被 docs/deployment/prod-mac-runbook.md 作为 cp 模板引用（已对齐双文件拆分）【上传GIT】
├── .env.backend.example     # 保留：被 docs/deployment/prod-mac-runbook.md 作为 cp 模板引用（已对齐双文件拆分）【上传GIT】
├── .env.prod                # 服务器生产环境变量【不传GIT】
├── .env.prod.backend        # 服务器生产环境变量（针对后端容器）【不传GIT】
├── .env.prod.example        # 保留：被 docs/deployment/prod-mac-runbook.md 作为 cp 模板引用（已对齐双文件拆分）【上传GIT】
├── .env.prod.backend.example # 保留：被 docs/deployment/prod-mac-runbook.md 作为 cp 模板引用（已对齐双文件拆分）【上传GIT】
├── .gitignore               # 屏蔽密钥、环境变量、日志、模型权重【上传GIT】
├── .gitattributes           # 待核验
├── .dockerignore            # 构建Docker镜像时统一屏蔽日志、模型、环境变量、编译产物，开发/生产Dockerfile共用【可上传GIT】
├── docker-compose.base.yml  # 基础业务容器编排，常驻公共中间件【开发与生产环境共用】：Nginx、MySQL、Redis,容器基础镜像完全独立，和业务代码无关【可上传GIT】
├── docker-compose.business-prj.dev.yml    # 业务容器编排：前端、后端【开发环境版本】【可上传GIT】
├── docker-compose.prod.yml  # 业务容器编排：前端、后端【生产环境版本】【可上传GIT】
├── VERSION.md               # 用 Markdown 格式清晰记录项目版本、迭代号、构建版本【可上传GIT】
└── 项目开发说明              # 项目 Readme 文档，记录开发、部署流程，给团队提供使用指引【可上传GIT】






# 在git-hub删除制定文件.env.dev
git filter-branch --force --index-filter 'git rm --cached --ignore-unmatch .env.dev' --prune-empty --tag-name-filter cat -- --all
# 1.清理本地 Git 垃圾（压缩旧历史）
git reflog expire --expire=now --all
git gc --prune=now --aggressive
# 2. 强制推送重写后的本地历史到远程（关键）
git push origin --force --all

git add VERSION.md
git rm "1.2.5-r12)" "26.0.1"
git commit -m "标准化版本文件为VERSION.md，清理旧版本零散文件"


4.资源配额规划（适配 16G 本地内存）
(1)常驻公共容器（开机长期运行，总占用≈3GB）
容器服务	    镜像版本	                内存限制	    CPU 限制	        作用说明
dev-nginx-gateway	nginx:1.25-alpine	        256M	        0.5 核	        统一网关、反向代理、跨域、静态分发
dev-mysql	        mysql:8.0	                1G	            1 核	        多项目共用，Bibutong 使用bibutong_dev库
dev-redis	        redis:5.0.14-alpine	      512M	        0.3 核	        共用缓存，Bibutong 固定使用 db9
dev--prj-llama	                         	  4G	            2 核	        Llama3.2 大模型 + bge-m3 向量嵌入，可临时关停释放内存
(2)业务容器（分时启停，总占用≈4GB）
容器服务	    镜像 / 构建规则	            内存限制	    CPU 限制	          备注
dev-prj-frontend	nginx:1.25-alpine	        128M	        0.2 核	        Vue 打包后静态托管，无 Node 运行时
dev-prj-backend-c	eclipse-temurin:21-jdk-alpine 构建	2G	2 核	         Spring Cloud Alibaba2.5.1，JVM -Xmx1800M
dev-prj-php

三、网络与端口规划（本地开发隔离，不和生产冲突）
1. 宿主机暴露端口（仅 127.0.0.1 访问）
服务	        宿主机端口	    容器内部端口	    用途
Nginx 网关 HTTP	   80	        80	            前端页面、API 统一入口
Nginx 网关 HTTPS	443	        443	            开发自签 SSL 调试
MySQL	            33060	    3306	        Navicat 本地连接开发库
Redis	            63790	    6379	        Redis 客户端调试缓存
Llama AI 推理	    11434	    11434	        后端本地调用向量接口
2. 容器内网互通规则
统一自定义网络dev-network，所有容器加入该网络；
数据库、Redis 仅允许网关、业务后端、AI 容器内网访问，不对外暴露；
禁止容器使用--privileged特权模式；
容器间通信使用服务名作为域名（mysql、redis、llama），无需 IP 写死。

具体生产环境的配置情况如下：1.硬件：mac mini m2 16+1T；2.系统：macOS Sonoma 14.8.7；3.功能：前后端服务器、数据库服务器、AI基础服务器；4.工具：OrbStack2.1.3（docker容器编排）

# 目录清理记录（2026-07-20）
- `ai_llama/` 目录已废弃移除：Ollama 推理服务自 2026-07-16 起迁至宿主机原生部署（Windows/macOS），不再使用容器构建，`ai_llama/Dockerfile.llama` 已删除。
- 原 `ai_llama/README-SECURITY.md`（SSH 密钥安全合规说明，记录私钥曾入 git、需轮换、需清理 git 历史等要求）已归档至 `docs/security/ssh-key-incident.md`，内容一字不差保留，未随目录丢弃。
- `docker-compose.base.yml` 中引用 `ai_llama/Dockerfile.llama` 的「容器版 Ollama」注释回退示例已移除（容器版 dev-prj-llama 服务自 2026-07-16 起已彻底移除）。
- 凭证（`.env.dev` / `.env.prod`）真实值未变动；dev 业务运行配置不变，仅清理上述注释示例。

# 目录清理记录（2026-07-20 补充）
- `docker/ollama/docker-entrypoint-llama.sh` 已删除：该脚本为容器化 Ollama 启动包装脚本（后台启动 ollama serve、等待就绪、幂等拉取 bge-m3 模型），因 Ollama 自 2026-07-16 起迁至宿主机原生部署（Windows/macOS），且 `docker-compose.base.yml` 中 `dev-prj-llama` 容器服务已于上一轮清理中移除，故该脚本已无调用方，废弃删除。
- 删除前 `docker/` 目录仅含 `ollama/` 子目录且其中仅此脚本，删除脚本后 `ollama/` 与 `docker/` 目录均为空，已一并移除（无 Dockerfile、.dockerignore 等其它文件）。
- 脚本内容已完整备份归档至 `docs/audit/archive/2026-07-20/xbox-cleanup-ollama-script-2026-07-20.bak.sh`，便于后续查阅，非直接丢弃。
- 全仓（主文件）已无可运行引用；仅备份 compose 文件（`docker-compose.base.yml.*.bak-2026-07-20`）、历史报告与本文档清理记录中保留描述性提及，属合理保留。
- 凭证（`.env.dev` / `.env.prod`）真实值未变动；dev 业务运行配置不变；`docker compose config` 合并不受影响（主 compose 已无 ollama 服务）。

