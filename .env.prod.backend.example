# ============================================================
# 生产后端专用环境变量模板（对应真实 .env.prod.backend）
# 使用: cp .env.prod.backend.example .env.prod.backend && 填入实际值（.env.prod.backend 已被 .gitignore 忽略，切勿提交）
#
# [C4] 最小暴露：本文件绝不含有任何数据库 ROOT 口令（MYSQL_ROOT_PASSWORD）。
# 由 docker-compose.prod.yml 的 prj-backend-c.env_file 注入后端容器。
# 注意：本文件仅被 env_file 注入后端容器，不经 --env-file 被 compose 插值，
#       故口令中的 '$' 本可直书；但 SPRING_DATASOURCE_PASSWORD 须与 .env.prod 的 PRJ_DB_PWD
#       完全一致，而后者经 compose 插值+my.cnf，故同样禁用 $ # ; 等字符（仅用 [A-Za-z0-9_@.-]）。
# 所有敏感值均为占位符，上线前必须替换为强随机值，切勿提交仓库。
# ============================================================

# === 数据源（业务账号 prj_user，非 ROOT）===
# 【R2】必须与 .env.prod 的 PRJ_DB_PWD 完全相同，否则 prod 后端连不上库。
SPRING_DATASOURCE_USERNAME=prj_user
SPRING_DATASOURCE_PASSWORD=ChangeMe_Prj

# === JWT（prod profile 强制 >=256-bit 强密钥）===
JWT_SECRET=ChangeMe_JwtSecret

# === Druid 监控账号 ===
DRUID_USERNAME=druid_admin
DRUID_PASSWORD=ChangeMe_Druid

# === Swagger（生产关闭）===
SWAGGER_ENABLED=false

# === AI 服务令牌（预留占位，当前未被后端消费）===
AI_API_TOKEN=ChangeMe_AiToken
